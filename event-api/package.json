{
  "name": "event-api",
  "version": "1.0.0",
  "description": "AWS Lambda functions for an events API.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "",
  "dependencies": {
    "dynamodb": "^1.3.0",
    "joi": "^17.6.0"
  }
}
